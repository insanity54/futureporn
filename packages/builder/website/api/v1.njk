---
layout: false
permalink: '/api/v1.json'
---
{
  "vods": [
    {% for vod in db.vods %}
      {
        "title": "{{ vod.title | urlencode }}",
        "date": "{{ vod.date }}",
      }{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}