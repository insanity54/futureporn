

    <div class="block">
      <section class="hero is-primary">
        <div class="hero-body">
          <p class="subtitle">Unofficial <a target="_blank" href="https://twitter.com/ProjektMelody">ProjektMelody <span class="icon"><i class="fas fa-external-link-alt"></i></span></a> Chaturbate VOD Archive. For Adults Only. (NSFW)</p>
        </div>
      </section>
    </div>

    <div class="news notification">
      <p>Any clippers? <span class="icon"><i class="fas fa-paperclip"></i></span><span class="icon"><i class="fas fa-paperclip"></i></span> Futureporn needs your help completing it's ProjektMelody VOD archive. {% ipfsProgressPercentage collections.vods %}</p>
      <p>If you have any ProjektMelody Chaturbate VODs that Futureporn is missing, please <a href="/upload">upload</a> them!</p>
      <p>All VOD contributors get their name listed on the site.</p>
    </div>

    <table class="table">
      <thead>
          <tr>
            <th>Date</th>
            <th>Video (Source)</th>
            {#<th>Video (720p)</th>#}
            {#<th>Video (480p)</th>#}
            {#<th>Video (360p)</th>#}
            <th>Video (240p)</th>
            {#<th>Audio Only</th>#}
            <th>Thumbnail</th>
            <th>Title</th>
            <th>Annouce URL</th>
          </tr>
        </thead>
        <tbody>
          {% for vod in vodslist | reverse %}
          <tr>
            <td><a href="{{ vod.url | url }}">{{ vod.data.date | htmlDateString }}</a></td>
            <td>
              {% if vod.data.videoSrcHash %}
                <a target="_blank" href="{% buildIpfsUrl vod.data.videoSrcHash %}">
                    <span>
                      <i class="fas fa-video"></i>
                    </span>
                    <span class="icon">
                      <i class="fas fa-external-link-alt"></i>
                    </span>
                </a>
              {% endif %}
            </td>

            {#<td>
              {% if vod.data.video720Hash %}
                <a target="_blank" href="{% buildIpfsUrl vod.data.video720Hash %}">
                    <span>
                      <i class="fas fa-video"></i>
                    </span>
                    <span class="icon">
                      <i class="fas fa-external-link-alt"></i>
                    </span>
                </a>
              {% endif %}
            </td>#}

            {#<td>
              {% if vod.data.video480Hash %}
                <a target="_blank" href="{% buildIpfsUrl vod.data.video480Hash %}">
                    <span>
                      <i class="fas fa-video"></i>
                    </span>
                    <span class="icon">
                      <i class="fas fa-external-link-alt"></i>
                    </span>
                </a>
              {% endif %}
            </td>#}

            {#<td>
              {% if vod.data.video360Hash %}
                <a target="_blank" href="{% buildIpfsUrl vod.data.video360Hash %}">
                    <span>
                      <i class="fas fa-video"></i>
                    </span>
                    <span class="icon">
                      <i class="fas fa-external-link-alt"></i>
                    </span>
                </a>
              {% endif %}
            </td>#}

            <td>
              {% if vod.data.video240Hash %}
                <a target="_blank" href="{% buildIpfsUrl vod.data.video240Hash %}">
                    <span>
                      <i class="fas fa-video"></i>
                    </span>
                    <span class="icon">
                      <i class="fas fa-external-link-alt"></i>
                    </span>
                </a>
              {% endif %}
            </td>

            {#<td>
              {% if vod.data.audioSrcHash %}
                <a target="_blank" href="{% buildIpfsUrl vod.data.audioSrcHash %}">
                    <span>
                      <i class="fas fa-file-audio"></i>
                    </span>
                    <span class="icon">
                      <i class="fas fa-external-link-alt"></i>
                    </span>
                </a>
              {% endif %}
            </td>#}

            <td>
              {% if vod.data.thiccHash %}
                <a target="_blank" href="{% buildIpfsUrl vod.data.thiccHash %}">
                  <span class="icon">
                    <i class="fas fa-image"></i>
                  </span>
                  <span class="icon">
                    <i class="fas fa-external-link-alt"></i>
                  </span>
                </a>
              {% endif %}
            </td>

            <td>
              {% if vod.data.announceTitle %}
                {{ vod.data.announceTitle | urlDecode }}
              {% elseif vod.data.title %}
                {{ vod.data.title | urlDecode }}
              {% endif %}
            </td>

            <td>
              {% if vod.data.announceUrl %}
                <a target="_blank" href="{{ vod.data.announceUrl }}">
                  <span class="icon">
                    <i class="fab fa-twitter"></i>
                  </span>
                  <span class="icon">
                    <i class="fas fa-external-link-alt"></i>
                  </span>
                </a>
              {% endif %}
            </td>

          </tr>
          {% endfor %}
        </tbody>

    </table>